import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Chocolate here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Chocolate extends Actor
{
    /**
     * Act - do whatever the Chocolate wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public Chocolate()
    {
    getImage().scale(getImage().getWidth() / 20 , getImage().getHeight() /20);
    }
    public void act()
    {
        // Add your action code here.
    }
}
